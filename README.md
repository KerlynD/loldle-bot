# loldle-bot

A Discord bot that shares the daily LoLdle puzzle with fun, randomized messages and server emotes!

## Features

- 🎮 `/loldle` command that provides a button to open today's LoLdle
- 🎲 Random cute messages every time someone uses the command
- 😄 Uses random emotes from your server to add personality
- 🌅 Automatically announces new LoLdle puzzles at 2am EST daily

## Usage

- Use `/loldle` in any channel to get today's LoLdle link with a cute random message
- The bot will automatically post in the announcement channel at 2am EST when a new LoLdle is available

## Example Messages

When someone uses `/loldle`, the bot will respond with messages like:
- "@User used `/loldle`, may your ranked games go well today 🎯"
- "@User used `/loldle`, time to test your League knowledge 🎮"
- "@User used `/loldle`, may your queues be short and your LP high ⭐"

(Plus it uses random emotes from your server!)
